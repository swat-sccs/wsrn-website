version: '3.8'

services:
  wsrn:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wsrn-docker
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules
